#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Return: is 0
 */
int main(void)
{
int n;

srand(time(0));
n = rand() - RAND_MAX / 2;

int x = n % 10 
 if (x > 5)
else if (x == 0)
 else if((x < 6) && (x > 0)


return (0);
}
